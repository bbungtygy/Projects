<li class="comment">
  <%= comment.commenter %>
  <%= link_to '수정', edit_comment_path(comment) %>
  <%= link_to '삭제', comment_path(comment), method: :delete, data: { confirm: '삭제?' } %>
  <%= comment.content %>
</li>
